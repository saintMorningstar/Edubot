#ifndef API_CLIENT_H
#define API_CLIENT_H

#include <Arduino.h>

// WiFi removed — API client is stubbed (not used in BLE architecture)
String sendAPIRequest(String endpoint);

#endif // API_CLIENT_H
